import React from 'react'

const Redirect = ({link}) => {
    return (
        <a href={link} target="_blank" rel="noopener noreferrer">  
        
        </a>
    )
}

export default Redirect;
